import { HightlightPipePipe } from './hightlight-pipe.pipe';

describe('HightlightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HightlightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
